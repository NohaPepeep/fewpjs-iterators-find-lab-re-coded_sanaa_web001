const testVar = {}

function testFunc() {
  return "hi"
}
function superbowlWin(arr){
  const newarr=arr.find(a.result==="W");
  if(newarr==="W"){
    return newarr.year;
  }
  else{
    return newarr;
  }
}